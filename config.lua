Config = {}    
-- Configure As Many Items as you want

-- Each Item can only perform One Chat Command (You cant add multiple commands for the same item)


Config.Setup = {
    [1] = {command = "revive", itemname = "lcd-lsd"},
    [2] = {command = "kill", itemname = "lcd-heroin"},
}
